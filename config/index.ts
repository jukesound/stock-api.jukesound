import table from '@config/table';
import routes from '@config/routes';
import httpCode from '@config/httpCode';
import ObserverTypeEnum from '@config/ObserverTypeEnum';

export default {
  table,
  routes,
  httpCode,
  ObserverTypeEnum,
};
